#ifndef STRUTILS_H
#define STRUTILS_H

// Đạo ngược chuỗi
char *str_reverse(char str[]);
// Loại bỏ khoảng trắng đầu và cuỗi chuỗi
void str_trim(char *str);
// Chuyển chuỗi sang số nguyên
int str_to_int(char str[]);
#endif
